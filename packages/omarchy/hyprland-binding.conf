# Optional user binding. Add only after reviewing the concrete install plan.
# Uses Omarchy's configured terminal rather than assuming a terminal emulator.
bindd = SUPER SHIFT, N, My Numi, exec, omarchy-launch-terminal my-numi --tui
# Native panel alternative (choose one binding):
# bindd = SUPER SHIFT, N, My Numi panel, exec, omarchy-shell shell toggle befeast.my-numi '{}'
